// Analytics disabled — this is a children's educational platform (COPPA compliance).
// No child usage data is sent to Google Analytics or any third-party service.

export async function event(_name: string, _params: Record<string, unknown> = {}) {
  // no-op
}
